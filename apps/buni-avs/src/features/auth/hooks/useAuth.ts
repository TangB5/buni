'use client';

// Re-export from the shared auth package
export { useAuth } from '@buni/auth';

// App-specific hooks are in separate files:
// - useLogin in ./useLogin.ts
// - useRegister in ./useRegister.ts
